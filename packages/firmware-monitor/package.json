{
  "name": "@z80emu/firmware-monitor",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@z80emu/assembler-z80": "file:../assembler-z80"
  },
  "scripts": {
    "generate": "node scripts/generate-bios-rom.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:compat": "vitest run tests/command-matrix.test.ts tests/compat-corpus.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
