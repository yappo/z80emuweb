; ============================================================================
; グラフィックス系命令スタブ
; ----------------------------------------------------------------------------
; 役割:
;   - 描画命令群の入口を保持する。
;   - 現段階は共通スタブで受け、将来ルーチン差し替え可能な形にする。
; ============================================================================

CMD_HDCOPY_HANDLER:
  RET

CMD_PAINT_HANDLER:
  RET

CMD_CIRCLE_HANDLER:
  RET

CMD_GCURSOR_HANDLER:
  RET

CMD_GPRINT_HANDLER:
  RET

CMD_LINE_HANDLER:
  RET

CMD_PSET_HANDLER:
  RET

CMD_PRESET_HANDLER:
  RET
