{
  "name": "@z80emu/assembler-z80",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "asm": "node dist/cli.js",
    "docs:mnemonics": "npm run build && node scripts/generate-mnemonics-doc.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
